---
layout: layouts/base.njk
title: "Build Sizes"
description: "Common simulator room sizes with tailored setup guidance."
permalink: /builds/
---

<section class="rounded-3xl border border-slate-200 bg-slate-100/70 px-8 py-12">
  <div class="max-w-3xl">
    <h1 class="text-4xl font-semibold tracking-tight text-slate-900 md:text-5xl">Common room builds</h1>
    <p class="mt-4 text-lg text-slate-600">Choose a size to see a tailored simulator setup and constraints.</p>
  </div>
</section>

<section class="py-16">
  <div class="space-y-6">
    {% for build in build_sizes %}
    <article class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition-all duration-200 ease-out hover:shadow-md">
      <h2 class="text-2xl font-semibold text-slate-900">
        <a class="transition-all duration-200 ease-out hover:text-slate-700" href="/builds/{{ build.slug }}/">{{ build.label }}</a>
      </h2>
      <p class="mt-3 text-sm text-slate-600">{{ build.notes }}</p>
      <a class="mt-4 inline-flex items-center text-sm font-semibold text-emerald-700 transition-all duration-200 ease-out hover:text-emerald-800" href="/builds/{{ build.slug }}/">
        View setup ->
      </a>
    </article>
    {% endfor %}
  </div>
</section>